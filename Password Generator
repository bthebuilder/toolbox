import secrets

def createXword():
    """Creates Xword"""
    Nrow = int(input("How many rows would you like ? \n >"))
    Ncol = int(input("How many cols would you like ? \n >"))
    print("\n")
    for i in range(Nrow):
        print(secrets.token_hex(Ncol))
    return ""

createXword()
